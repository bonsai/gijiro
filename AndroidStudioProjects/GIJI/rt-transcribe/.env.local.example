# OpenAI API Key (required)
OPENAI_API_KEY=""

# (Optional) Model for summarization and other text tasks
# If empty, will use gpt-4o-mini
OPENAI_TEXT_MODEL="gpt-4o-mini"

# (Optional) Speech-to-Text model
# If empty, will try gpt-4o-mini-transcribe first, then fallback to whisper-1
OPENAI_STT_MODEL="gpt-4o-mini-transcribe"
